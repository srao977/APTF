# Test 013B Next-Test Recommendation V0.1

Use DIA as the next predesignated independent replication instrument with F4_L1_W30, lambda 1, state `[P,P1,P2]`, and RK45 unchanged. Do not execute DIA inside Test 013B.

The remaining mathematical concern is not endpoint/tail explosion; it is the combination of approximately 45% local-envelope exits, low transition recall, and positive local max-real eigenvalues despite much smaller magnitude. Preserve those diagnostics as explicit replication gates.

Do not begin cockpit color thresholds or Runtime promotion in parallel. A third-instrument replication should precede cockpit-envelope work because thresholding a conditionally validated transition/local-domain surface would overstate the current evidence.
